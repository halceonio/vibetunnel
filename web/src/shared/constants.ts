/**
 * Shared constants used across both client and server
 */

// Default paths
export const DEFAULT_REPOSITORY_BASE_PATH = '/shared';

// Server configuration
export const DEFAULT_SERVER_PORT = 4020;
